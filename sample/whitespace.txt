foo not   // This is an example where whitespace is important. If
          // whitespace was disregarded, this would be tokenized as the
          // id "foonot" rather the id "foo" and the keyword "not"
